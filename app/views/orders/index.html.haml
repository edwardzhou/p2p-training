%h1 我报名的课程

#orders
  %table
    %thead
      %tr
        %th
          订单号
        %th
          报名日期
        %th
          课程
        %th
          价格
        %th
          备注
        %th
          状态
        %th/

    %tbody
      - @orders.each do |order|
        %tr
          %td= order.order_code
          %td= order.created_at
          %td
            = order.campaign.course.course_name
            %br/
            = order.campaign.name
          %td= order.total_amount
          %td= order.comment
          %td= human_status('order', order.status)
          %td
            - if order.status == Order::Status::PENDING_PAYMENT
              = link_to "现在付款", confirm_payment_order_path(order)
            &nbsp;| 修改 |
            = link_to "取消", cancel_order_path(order), :method => :cancel
